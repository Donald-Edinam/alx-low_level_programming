#include <main.h>

/**
 * print_alphabet - prints the alphabet in lowercase, followwd by a new line
 * Return: Always 0
*/

void print_alphabet(void)
{
    char value;

    for (value = 'a'; value <= 'z'; value++)
        _putchar(letter);

    _putchar('\n');
}